export type SectionHeadModel = {
  title: string;
  icon: JSX.Element;
};

export type CardModel = {
  className: string;
  children: any;
};

export type FaqModel = {
  question: string;
  answer: string;
};
